const _config = {
  server: {
    host: 'localhost',
    port: 4444,
    open: false,
  },

  preview: {
    host: 'localhost',
    port: 3333,
    open: false,
  },

  root: './',
};

export default _config;
